# Recipe 标题

一句话说明这个示例做什么。

## 场景

说明用户遇到的问题，以及这个示例会跑出什么结果。

## 运行方式

```bash
uv run python main.py
```

需要环境变量时，放在这里：

```bash
export MODEL_API_KEY="..."
export MODEL_BASE_URL="..."
export MODEL_NAME="..."
```

## 代码结构

```text
README.md
main.py
.env.example
```

## 关键代码

```python
# 放最小关键片段。完整代码仍以 main.py 为准。
```

## 说明

解释这个 recipe 里的关键对象、状态、工具或图结构。只解释读者理解示例必须知道的部分。

## 可以怎么改

- 替换模型
- 替换工具
- 替换存储
- 接入 LangGraph

## 常见问题

### 问题标题

说明现象、原因和处理方式。
