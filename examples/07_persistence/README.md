# 07 persistence

## 场景

agent 进入长期运行后，不能只把状态留在一次进程里。

这一章预留给外置持久化示例。它会说明怎么保存 thread、checkpoint 和运行中的 state。进程重启后，agent 可以从已有状态继续。

## 代码

待补。

## 运行方式

待补。

## 关键点

- 区分长期 memory 和运行 checkpoint。
- 把 thread id、用户 id 和任务 id 的关系说清楚。
- 说明哪些状态要持久化，哪些状态只适合留在本次请求里。

## 取舍

持久化适合恢复现场和审计运行过程。不要把所有中间变量都写进外部存储。写入越多，恢复逻辑和数据清理越复杂。
