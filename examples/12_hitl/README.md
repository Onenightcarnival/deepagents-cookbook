# 12 HITL

## 场景

有些动作不能让 agent 自己决定。

这一章预留给 human in the loop 示例。它会说明怎么在关键节点加入人工确认、审批、打断和继续，让 agent 可以处理高风险步骤。

## 代码

待补。

## 运行方式

待补。

## 关键点

- 找出需要人工确认的步骤。
- 说明确认请求里要带哪些上下文。
- 说明用户拒绝、修改和超时后怎么处理。
- 和持久化配合，让等待人工确认的任务可以恢复。

## 取舍

HITL 适合发布、删除、付款、外部写入和权限变更这类动作。普通读操作不要都加确认，否则 agent 会变成问答表单。
